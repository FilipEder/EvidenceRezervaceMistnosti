@using EvidenceRezervaceMistnosti.DTO.PartialView

@model RoomFilterDTO

<!-- Vyhledávání -->
<div class="search-container mb-3">
    <label class="form-label" for="searchInput">
        Vyhledávání
    </label>
    <input id="searchInput"
           class="form-control"
           name="search"
           value=""
           placeholder="Vyhledejte místnost, vybavení nebo patro..."
           type="search">
</div>
<!-- Kapacita -->
<div class="filter-section" id="CapacityFilter">
    <div class="filter-header">
        Kapacita
    </div>
    <div class="row g-2">
        <div class="col-6">
            <label class="form-label" for="CapacityFromInput">Od</label>
            <input id="CapacityFromInput"
                   class="form-control"
                   type="number"
                   name="roomFilter.CapacityFrom"
                   min="1"
                   placeholder="1">
        </div>
        <div class="col-6">
            <label class="form-label" for="CapacityToInput">Do</label>
            <input id="CapacityToInput"
                   class="form-control"
                   type="number"
                   name="roomFilter.CapacityTo"
                   min="1"
                   placeholder="20">
        </div>
    </div>
</div>
<!-- Umístění -->
<div class="filter-section" id="LocationFilter">
    <div class="filter-header">
        Umístění
    </div>

    <label class="form-label" for="LocationSelect">Patro</label>
    <select id="LocationSelect" class="form-select" name="roomFilter.LocationId">
        @if (Model.Locations == null || !Model.Locations.Any())
        {
            <option value="" disabled>Žádná patra k dispozici</option>
        }
        else
        {
            <option value="" selected>Všechna patra</option>
            foreach (var floor in Model.Locations)
            {
                <option value="@floor.LocationId">@floor.LocationName</option>
            }
        }
    </select>
</div>
<!-- Vybavení -->
<div class="filter-section" id="EquipmentFilter">
    <div class="filter-header">
        Vybavení
    </div>

    <label class="form-label" for="EquipmentSelect">
        Typ vybavení
    </label>

    <select id="EquipmentSelect" class="form-select" name="EquipmentId">
        @if (Model.Equipments == null || !Model.Equipments.Any())
        {
            <option value="" disabled>Žádné vybavení k dispozici</option>
        }
        else
        {
            <option value="" selected>Všechna vybavení</option>
            foreach (var equipment in Model.Equipments)
            {
                <option value="@equipment.EquipmentId">@equipment.EquipmentName</option>
            }
        }
    </select>
</div>